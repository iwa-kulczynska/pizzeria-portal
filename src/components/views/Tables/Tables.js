import React from 'react';
import styles from './Tables.scss';
//import PropTypes from 'prop-types';

const Tables = () => (
  <div className={styles.component}>
    <h2 className={styles.title}>Tables views</h2>
  </div>
);

export default Tables;