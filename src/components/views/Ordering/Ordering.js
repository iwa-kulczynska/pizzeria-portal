import React from 'react';
import styles from './Ordering.scss';
//import PropTypes from 'prop-types';

const Ordering = () => (
  <div className={styles.component}>
    <h2 className={styles.title}>Ordering views</h2>
  </div>
);

export default Ordering;