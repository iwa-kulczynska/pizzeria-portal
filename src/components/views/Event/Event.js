import React from 'react';
import styles from './Event.scss';
//import PropTypes from 'prop-types';

const Event = () => (
  <div className={styles.component}>
    <h2 className={styles.title}>Event views</h2>
  </div>
);

export default Event;