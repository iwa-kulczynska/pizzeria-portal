import React from 'react';
import styles from './New.scss';
//import PropTypes from 'prop-types';

const New = () => (
  <div className={styles.component}>
    <h2 className={styles.title}>New views</h2>
  </div>
);

export default New;