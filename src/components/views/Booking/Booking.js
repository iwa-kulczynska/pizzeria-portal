import React from 'react';
import styles from './Booking.scss';
//import PropTypes from 'prop-types';

const Booking = () => (
  <div className={styles.component}>
    <h2 className={styles.title}>Booking views</h2>
  </div>
);

export default Booking;