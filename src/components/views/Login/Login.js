import React from 'react';
import styles from './Login.scss';
//import PropTypes from 'prop-types';

const Login = () => (
  <div className={styles.component}>
    <h2 className={styles.title}>Login views</h2>
  </div>
);

export default Login;