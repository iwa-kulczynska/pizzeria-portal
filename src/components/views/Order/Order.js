import React from 'react';
import styles from './Order.scss';
//import PropTypes from 'prop-types';

const Order = () => (
  <div className={styles.component}>
    <h2 className={styles.title}>Order views</h2>
  </div>
);

export default Order;